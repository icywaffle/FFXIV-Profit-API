# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs

# We need to separate the REST API endpoints from the User web application.
# We can just use a /api/
GET     /api/                                       ItemInfo.Index
GET     /api/documentation/                         ItemInfo.Index
GET     /api/recipe/:recipeID                       ItemInfo.Obtain
POST    /api/userinfo/:userid                       UserInfo.Store
GET     /api/userinfo/:userid/recipe/:recipeid      UserInfo.Obtain
# Ignore favicon requests
GET     /api/favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /api/public/*filepath                       Static.Serve("public")

# Allows React scripts to work since we need to expose all the local script files.
GET     /api/Scripts/*filepath                      Static.Serve("Scripts")

# Catch all, this will route any request into the controller path
#
#                    **** WARNING ****
# Enabling this exposes any controller and function to the web.
# ** This is a serious security issue if used online **
#
# For rapid development uncomment the following to add new controller.action endpoints
# without having to add them to the routes table.
# *       /:controller/:action                    :controller.:action
